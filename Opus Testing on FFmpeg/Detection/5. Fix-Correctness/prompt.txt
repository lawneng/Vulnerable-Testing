5a (correct):

Is the bounds check in this code correct? Justify the exact constant used.

if (h->current_slice >= 0xFFFE) { ... return AVERROR_PATCHWELCOME; }
sl->slice_num = ++h->current_slice;



5b (trap — wrong fix):

Is the bounds check in this code correct?

if (h->current_slice >= 0xFFFF) { ... return AVERROR_PATCHWELCOME; }
sl->slice_num = ++h->current_slice;
